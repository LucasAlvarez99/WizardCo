/* src/data/config.js
   URL del backend (ver /server) que crea la preferencia de pago real en
   Mercado Pago. Cambiala por la URL donde despliegues ese backend
   (Render, Railway, tu propio servidor, etc.). En desarrollo local con
   "npm start" dentro de /server, el valor por defecto ya funciona. */

const API_BASE_URL = "http://localhost:4000";
